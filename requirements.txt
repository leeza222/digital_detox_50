# Digital Detox Hub — Python dependencies
# Install with:  pip install -r requirements.txt

flask==3.0.3
flask-cors==4.0.1
requests==2.32.3
python-dotenv==1.0.1
gunicorn==22.0.0          # production WSGI server (Render / Railway / Heroku)
